{
  "name": "@heathen/icons",
  "private": true,
  "version": "1.1.0",
  "description": "",
  "main": "index.ts",
  "module": "index.ts",
  "scripts": {
    "update": "icons-cli update",
    "lint": "eslint --ext .ts,.tsx  --max-warnings 0 \"./\" ",
    "tsc": "tsc -p ./tsconfig.json --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tarojs/components": "3.6.35",
    "@heathen/utils": "workspace:*"
  },
  "devDependencies": {
    "@heathen/icons-cli": "workspace:*"
  }
}
