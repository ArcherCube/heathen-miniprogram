import path from 'path';

export const MAIN_PACKAGE_NAME = 'main' as const;

export const TARGET_MODULE_PATH = path.resolve('node_modules', '@heathen/router');

export const SOURCE_PATH = path.resolve('./src');
