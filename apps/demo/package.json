{
  "name": "@heathen-apps/demo",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "postinstall": "weapp-tw patch && taro-router-helper update",
    "build:weapp": "taro build --type weapp",
    "dev:weapp": "npm run build:weapp -- --watch",
    "lint": "eslint --ext .ts,.tsx  --max-warnings 0 \"src\" \"config\" ",
    "stylelint": "stylelint \"src/**/*.css\"",
    "tsc": "tsc -p ./tsconfig.json --noEmit"
  },
  "browserslist": [
    "last 3 versions",
    "Android >= 4.1",
    "ios >= 8"
  ],
  "author": "",
  "dependencies": {
    "@heathen/colors": "workspace:*",
    "@heathen/components": "workspace:*",
    "@heathen/hooks": "workspace:*",
    "@heathen/icons": "workspace:*",
    "@heathen/polyfill": "workspace:*",
    "@heathen/utils": "workspace:*",
    "@heathenjs/taro-router": "1.1.5"
  },
  "devDependencies": {
    "@heathenjs/taro-router-cli": "1.1.5",
    "@heathenjs/taro-router-plugin": "1.1.5"
  }
}
